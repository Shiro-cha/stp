const bzr = require("../bazar.function")

module.exports={
  //set configuration
  setConfiguration:options=>{
    console.log(options)
  },
  resetConfiguration:options=>{
    console.log(options)
  }
}
